// Moved from src/skills/executor.rs into contrib. Kept for reference.
use anyhow::Result;
use tracing::{info, debug};

#[allow(dead_code)]
use crate::skills::registry::{SkillDefinition, SkillsRegistry};

pub struct SkillExecutor;

impl SkillExecutor {
    // archived executor
}
