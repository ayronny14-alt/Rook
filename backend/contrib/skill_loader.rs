// Moved from src/skills/loader.rs into contrib. Kept for reference.
#[allow(dead_code)]
use anyhow::Result;
use std::path::Path;
use tracing::{debug, info, warn};

use crate::skills::registry::{SkillDefinition, SkillsRegistry};

pub struct SkillLoader;

impl SkillLoader {
    // original methods preserved for reference
}
